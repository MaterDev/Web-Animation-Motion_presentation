# dependencies
node_modules/

# build output
dist/
build/
.vite/
/.svelte-kit
.output
.netlify
.vercel
.wrangler

# generated: copied from design/ and node_modules by scripts/
static/sheets/
design/vendor/

# env
.env
.env.local
.env.*.local
.env.*
!.env.example

# tooling / scratch
.playwright-mcp/
test-results/
playwright-report/
vite.config.js.timestamp-*

# editor / os
.DS_Store
.idea/
*.swp

# claude code: machine-local settings (launch.json is shared, this is not)
.claude/settings.local.json
# borrowed-agent scaffolding — ephemeral, not part of this project
.claude/agent-memory/

# NOTE: conductor/ and .claude/commands/ ARE committed here, deliberately.
# The portable setup spec (~/.claude/conductor-setup-prompt.md) git-ignores
# them; this repo keeps them so the reasoning behind a rebuild lives beside
# the code it produced. See conductor/README.md "Deviations".
