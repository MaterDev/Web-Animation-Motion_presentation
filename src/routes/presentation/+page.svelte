<script>
  /* The deck, on its grid. The deck itself lives in $lib/deck/Deck.svelte
     so every slide can also be reached at its own URL — see [slide]. */
  import Deck from '$lib/deck/Deck.svelte';
</script>

<Deck />
